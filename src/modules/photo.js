class Photo {
  cosntructor(id,author,width,heigth,url,download_url) {
    this.id = id;
    this.author = author;
    this.width = width;
    this.heigth = heigth;
    this.url = url;
    this.download_url = download_url
  }
}

export default Photo;